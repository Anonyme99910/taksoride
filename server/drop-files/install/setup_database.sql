-- TaksoRide Database Setup Script
-- This script creates the database and imports the schema

-- Create database if it doesn't exist
CREATE DATABASE IF NOT EXISTS `hamma` CHARACTER SET utf8 COLLATE utf8_unicode_ci;

-- Use the database
USE `hamma`;

-- Note: After running this, import the database_setup.sql file
-- You can do this via phpMyAdmin or command line:
-- mysql -u root -p hamma < database_setup.sql
